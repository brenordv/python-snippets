[project]
name = "fun-with-dates"
version = "1.0.0"
description = "Demonstrations of date parsing, uptime calculation, and relative dates in Python."
requires-python = ">=3.10"
dependencies = [
    "python-dateutil>=2.8",
]
