# Fun with Dates

A collection of snippets demonstrating common date/time operations in Python: parsing strings into `datetime` objects, computing uptime (timedelta between two dates), and calculating relative dates with `dateutil.relativedelta`.

## Usage

```bash
python convert_str_to_datetime.py
python get_uptime.py
python relative_dates.py
```
