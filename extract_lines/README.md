# Extract Lines

Reads a file line-by-line, finds lines containing a given search term, and writes the matching lines to an output file. Useful for filtering log files or large text files.

## Usage

```bash
# Edit the input/output paths and search term in the script, then run:
python extract_lines.py
```
