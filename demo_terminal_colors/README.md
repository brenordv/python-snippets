# Demo Terminal Colors

Displays all 16 standard ANSI terminal colors (normal and bright variants) with both foreground and background samples. Useful as a quick reference for terminal color codes.

## Usage

```bash
python terminal_colors.py
```
