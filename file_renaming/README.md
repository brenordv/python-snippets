# File Renaming

Batch-rename files that follow a three-part naming convention (`Part1 - Part2 - Part3.ext`) by dropping the third part and joining the first two with double underscores. Supports a dry-run mode to preview changes before applying them.

## Usage

```bash
# Edit the path in the script, then run:
python rename_files.py
```
