[project]
name = "controlling-servo-motors"
version = "1.0.0"
description = "Control hobby servos using a Raspberry Pi and PCA9685 PWM driver via I2C."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "adafruit-circuitpython-servokit==1.3.22",
]
