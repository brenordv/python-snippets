# Import from Directory

Shows how to import all modules from a subdirectory by making it a Python package (adding `__init__.py`). Also demonstrates that a directory *without* `__init__.py` (like `otherfiles/`) cannot be imported the same way.

## Usage

```bash
python import_from_dir.py
```
