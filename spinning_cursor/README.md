# Spinning Cursor

A simple spinning cursor animation for terminal output. Uses `itertools.cycle` to rotate through spinner frames (`\ | / -`) and displays them alongside a status label.

## Usage

```bash
python spinning.py
```
