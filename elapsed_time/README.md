# Elapsed Time

A small utility that computes and displays formatted elapsed time (HH:MM:SS.ss). Includes a demo that simulates work with a random sleep duration and prints the elapsed time at the end.

## Usage

```bash
python elapsed_time.py
```
