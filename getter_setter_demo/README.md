# Getter/Setter Demo

Shows how to use Python `@property` decorators to add validation logic to attribute access. The `User` class rejects `None` values on every field and tracks whether the object has been modified since creation.

## Usage

```bash
python getter_setter_demo.py
```
