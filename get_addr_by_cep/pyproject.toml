[project]
name = "get-addr-by-cep"
version = "1.0.0"
description = "Progressive examples of looking up Brazilian addresses by CEP (postal code) via a public API."
requires-python = ">=3.10"
dependencies = [
    "requests==2.33.1",
]
